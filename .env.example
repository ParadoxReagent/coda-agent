# Database
# Note: When running via Docker, DATABASE_URL is overridden automatically
# to use the postgres container. This value is used for local development only.
DATABASE_URL=postgresql://coda:coda@localhost:5432/coda
POSTGRES_PASSWORD=coda

# Redis
# Note: When running via Docker, REDIS_URL is overridden automatically
# to use the redis container. This value is used for local development only.
REDIS_URL=redis://localhost:6379

# Discord
DISCORD_BOT_TOKEN=
DISCORD_CHANNEL_ID=
DISCORD_ALLOWED_USER_IDS=

# Slack (optional — also configurable in config.yaml)
# SLACK_APP_TOKEN=
# SLACK_BOT_TOKEN=
# SLACK_CHANNEL_ID=
# SLACK_ALLOWED_USER_IDS=

# Telegram (optional — also configurable in config.yaml)
# Get bot token from @BotFather; chat_id from getUpdates API
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=
# TELEGRAM_ALLOWED_USER_IDS=

# LLM Providers (set in config.yaml or override here)
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=

# Tier Routing — configure in config.yaml under llm.tiers (preferred)
# These env vars still work as overrides if needed:
# TIER_ENABLED=true
# TIER_LIGHT_MODEL=anthropic/claude-haiku-4.5
# TIER_HEAVY_MODEL=anthropic/claude-sonnet-4.5
# TIER_LIGHT_PROVIDER=openrouter
# TIER_HEAVY_PROVIDER=openrouter

# Memory Service
MEMORY_API_KEY=

# Firecrawl (web scraping/crawling) - API v2
# Cloud: set FIRECRAWL_API_KEY (get key from https://firecrawl.dev)
# Self-hosted: set FIRECRAWL_API_URL (e.g., http://localhost:3002)
# See: https://github.com/firecrawl/firecrawl
FIRECRAWL_API_KEY=
FIRECRAWL_API_URL=

# n8n Webhook Service (for inbound events from n8n)
# Generate secret with: openssl rand -base64 32
N8N_WEBHOOK_SECRET=
N8N_WEBHOOK_HEADER_NAME=x-webhook-secret

# REST API Authentication
API_KEY=

# MCP (Model Context Protocol) Servers
# Used in config.yaml with ${VAR_NAME} syntax
# Example: url: ${MCP_GITHUB_URL}
# MCP_GITHUB_URL=https://mcp-github.example.com
# GITHUB_TOKEN=ghp_xxxxxxxxxxxx
# MCP_DATABASE_URL=http://mcp-db:8080
# MCP_FS_TOKEN=your-filesystem-token

# MCP Server Docker Images
# Some MCP servers run as Docker containers (e.g., Context7)
# Build images first: npm run build:mcp-images
# These servers typically don't require environment variables

# Application
NODE_ENV=development
LOG_LEVEL=info
CONFIG_PATH=./config/config.yaml
